# 19 - حالات الحافة (Edge Cases)

## 1. مستخدم شرعي يقوم بمعاملة كبيرة

```php
// القاعدة: > 5000 USD → مراجعة
// الحالة: تاجر يشتري بضاعة بـ 10000 USD
// الحل: إمكانية توثيق المعاملة مسبقاً (whitelist)
```

## 2. مستخدم يسافر (IP يتغير)

```php
// القاعدة: IP جديد → إشعار
// الحالة: مستخدم يفتح التطبيق من دولة أخرى
// الحل: إضافة "تذكر هذا الجهاز" (session token)
```

## 3. PIN صحيح بعد 4 محاولات خاطئة

```php
// المحاولات الخاطئة: 4
// المحاولة الخامسة: PIN صحيح
// النتيجة: إعادة تعيين العداد
// الحل: reset counter على النجاح
```

## 4. محاولة اختراق متعمدة (1000 محاولة PIN)

```php
// IP محظور تلقائياً بعد 20 محاولة فاشلة
// الحساب مقفول ويتطلب دعم فني
```

## 5. أطفال يلعبون بالتطبيق

```php
// محاولات PIN سريعة ومتكررة
// الكشف: > 10 محاولات في دقيقة → CAPTCHA
```

## 6. معاملة مشبوهة في وقت غير معتاد

```php
// قاعدة إضافية: معاملة بين 2AM - 5AM
// الكشف: وقت غير معتاد + مبلغ كبير → مراجعة
```

## 7. التحويلات المتكررة (Smurfing)

```php
// تقسيم 5000 USD إلى 10 تحويلات 500 USD
// الكشف: مجموع التحويلات في ساعة > 3000 USD → مراجعة
```

## 8. إيجابية كاذبة (False Positive)

```php
// تقليل الإيجابيات الكاذبة:
// 1. التعلم من قرارات المشرفين السابقة
// 2. whitelist للمستخدمين الموثوقين
// 3. ضبط score thresholds
```
