# 03 - تدفق البيانات (Sequence Diagram)

```
  Admin            React Admin            Laravel API         ReportService       MySQL
   │                    │                     │                    │               │
   │  فتح التقرير      │                     │                    │               │
   │  اليومي           │                     │                    │               │
   │───────────────────>│                     │                    │               │
   │                    │  GET /admin/reports │                    │               │
   │                    │  /daily?date=       │                    │               │
   │                    │  2026-05-27         │                    │               │
   │                    │────────────────────>│                    │               │
   │                    │                     │  DailyReport       │               │
   │                    │                     │  Service::generate │               │
   │                    │                     │───────────────────>│               │
   │                    │                     │                    │               │
   │                    │                     │  معاملات اليوم     │               │
   │                    │                     │  SELECT COUNT,SUM  │               │
   │                    │                     │  FROM transactions │──────────────>│
   │                    │                     │  WHERE DATE=       │               │
   │                    │                     │                    │               │
   │                    │                     │  مستخدمون جدد      │               │
   │                    │                     │  SELECT COUNT FROM │               │
   │                    │                     │  users WHERE       │               │
   │                    │                     │  DATE(created_at)  │──────────────>│
   │                    │                     │                    │               │
   │                    │                     │  الإيرادات (رسوم)  │               │
   │                    │                     │  SELECT SUM(amount)│               │
   │                    │                     │  FROM transactions │               │
   │                    │                     │  WHERE type='fee'  │──────────────>│
   │                    │                     │                    │               │
   │                    │                     │  توزيع حسب النوع   │               │
   │                    │                     │  GROUP BY type     │──────────────>│
   │                    │                     │                    │               │
   │                    │                     │<───────────────────│               │
   │                    │<────────────────────│                    │               │
   │<───────────────────│                     │                    │               │
```

## جدول زمني

| التقرير | الزمن |
|---------|-------|
| يومي (مع فلترة) | ~200ms |
| شهري (مع مقارنة) | ~500ms |
| مالي (تحليل كامل) | ~800ms |
