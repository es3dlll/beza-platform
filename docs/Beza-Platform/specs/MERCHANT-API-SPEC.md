# مواصفات واجهة برمجة التطبيقات — مدفوعات التجار

> هذا الملف هو المرجع الوحيد للتكامل الخارجي مع نظام مدفوعات التجار في Beza.
> يغطي جميع نقاط API المطلوبة لدمج متجر إلكتروني أو تطبيق جوال مع بوابة الدفع.
> لكل نقطة: المسار، الطريقة، المدخلات، المخرجات، رموز الحالة، وأمثلة طلب واستجابة.

---

## فهرس النقاط

| # | النقطة | الطريقة | المسار | الحالة |
|---|--------|--------|--------|-------|
| 01 | تسجيل تاجر جديد | POST | `/api/v1/merchant/auth/register` | ✅ موثقة |
| 02 | تسجيل الدخول | POST | `/api/v1/merchant/auth/login` | ✅ موثقة |
| 03 | تجديد التوكن | POST | `/api/v1/merchant/auth/refresh` | ✅ موثقة |
| 04 | تسجيل الخروج | POST | `/api/v1/merchant/auth/logout` | ✅ موثقة |
| 05 | سرد المفاتيح | GET | `/api/v1/merchant/keys` | ⬜ عنوان فارغ |
| 06 | إنشاء مفتاح | POST | `/api/v1/merchant/keys/create` | ⬜ عنوان فارغ |
| 07 | إلغاء مفتاح | DELETE | `/api/v1/merchant/keys/{key_id}` | ⬜ عنوان فارغ |
| 08 | إنشاء رابط دفع | POST | `/api/v1/merchant/payment/create` | ⬜ عنوان فارغ |
| 09 | تأكيد الدفع | POST | `/api/v1/merchant/payment/process/{reference}` | ⬜ عنوان فارغ |
| 10 | إرجاع/مرتجع | POST | `/api/v1/merchant/payment/refund` | ⬜ عنوان فارغ |
| 11 | توليد رمز QR | POST | `/api/v1/merchant/qr/generate` | ✅ موثقة |
| 12 | التحقق من رمز QR | POST | `/api/v1/merchant/qr/verify` | ✅ موثقة |
| 13 | سرد التسويات | GET | `/api/v1/merchant/settlements` | ⬜ عنوان فارغ |
| 14 | تفاصيل التسوية | GET | `/api/v1/merchant/settlements/{id}` | ⬜ عنوان فارغ |
| 15 | تقارير المبيعات | GET | `/api/v1/merchant/reports/sales` | ⬜ عنوان فارغ |
| 16 | تقارير طرق الدفع | GET | `/api/v1/merchant/reports/payment-methods` | ⬜ عنوان فارغ |

---

## 01. المصادقة — تسجيل تاجر جديد

### POST /api/v1/merchant/auth/register

إنشاء حساب تاجر جديد. يتطلب توثيقاً تجارياً للموافقة من قبل المشرف.

#### المدخلات

| الحقل | النوع | الإلزام | الوصف |
|-------|-------|--------|-------|
| `business_name` | string(128) | مطلوب | الاسم التجاري |
| `owner_name` | string(128) | مطلوب | اسم صاحب التاجر |
| `phone` | string(15) | مطلوب | رقم الهاتف (مع رمز الدولة) |
| `email` | email | مطلوب | البريد الإلكتروني |
| `password` | string(64) | مطلوب | كلمة المرور (8+ أحرف، حرف كبير + رقم) |
| `commercial_register` | string(64) | مطلوب | رقم السجل التجاري |
| `tax_id` | string(64) | اختياري | الرقم الضريبي |
| `business_type` | enum | مطلوب | individual / company / ecommerce |
| `address` | string(256) | اختياري | العنوان الكامل |
| `city` | string(64) | اختياري | المدينة |
| `website` | url | اختياري | الموقع الإلكتروني |

#### الاستجابة (201 Created)

```json
{
  "success": true,
  "data": {
    "merchant_id": "merch_a1b2c3d4",
    "business_name": "متجر أحمد الإلكتروني",
    "status": "pending_approval",
    "message": "تم تسجيل حساب التاجر بنجاح. ينتظر الموافقة من المشرف."
  }
}
```

#### رموز الحالة

| الرمز | المعنى |
|-------|--------|
| 201 | تم إنشاء حساب التاجر — ينتظر الموافقة |
| 400 | خطأ في المدخلات |
| 409 | تعارض — رقم الهاتف أو البريد الإلكتروني مسجل مسبقاً |
| 422 | كيان غير قابل للمعالجة — سجل تجاري غير صالح |

---

## 02. المصادقة — تسجيل الدخول

### POST /api/v1/merchant/auth/login

تسجيل دخول التاجر واستلام Bearer Token للوصول إلى نقاط API.

#### المدخلات

| الحقل | النوع | الإلزام | الوصف |
|-------|-------|--------|-------|
| `phone` | string(15) | مطلوب | رقم الهاتف المسجل |
| `password` | string(64) | مطلوب | كلمة المرور |

#### الاستجابة (200 Success)

```json
{
  "success": true,
  "data": {
    "token_type": "Bearer",
    "access_token": "eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...",
    "expires_in": 1440,
    "merchant": {
      "id": "merch_a1b2c3d4",
      "business_name": "متجر أحمد الإلكتروني",
      "status": "approved",
      "tier": "medium",
      "daily_limit": 50000.00,
      "monthly_limit": 500000.00
    }
  }
}
```

#### رموز الحالة

| الرمز | المعنى |
|-------|--------|
| 200 | نجاح — تم تسجيل الدخول |
| 401 | غير مصرح — رقم الهاتف أو كلمة المرور خطأ |
| 403 | ممنوع — الحساب معلق أو لم تتم الموافقة عليه بعد |
| 429 | تجاوز المعدل — أكثر من 5 محاولات فاشلة/دقيقة |

---

## 03. المصادقة — تجديد التوكن

### POST /api/v1/merchant/auth/refresh

تجديد Bearer Token قبل انتهاء صلاحيته.

#### المدخلات

| الحقل | النوع | الإلزام | الوصف |
|-------|-------|--------|-------|
| `refresh_token` | string | مطلوب | توكن التجديد |

#### الاستجابة (200 Success)

```json
{
  "success": true,
  "data": {
    "token_type": "Bearer",
    "access_token": "eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...",
    "expires_in": 1440
  }
}
```

#### رموز الحالة

| الرمز | المعنى |
|-------|--------|
| 200 | نجاح — تم تجديد التوكن |
| 401 | غير مصرح — توكن التجديد منتهٍ أو غير صالح |

---

## 04. المصادقة — تسجيل الخروج

### POST /api/v1/merchant/auth/logout

إبطال التوكن الحالي وتسجيل الخروج.

#### المدخلات

لا يتطلب جسم طلب. يكفي إرسال Bearer Token في الترويسة.

#### الاستجابة (200 Success)

```json
{
  "success": true,
  "message": "تم تسجيل الخروج بنجاح"
}
```

#### رموز الحالة

| الرمز | المعنى |
|-------|--------|
| 200 | نجاح — تم إبطال التوكن |
| 401 | غير مصرح — التوكن غير صالح |

---

## 05. إدارة المفاتيح — سرد المفاتيح

> ⬜ **قيد التعبئة** — تُدرج هنا مفاتيح API الخاصة بالتاجر مع الحالة (نشط/ملغي).

---

## 06. إدارة المفاتيح — إنشاء مفتاح

> ⬜ **قيد التعبئة** — يُنشأ مفتاح API جديد مع صلاحيات محددة (قراءة/كتابة).

---

## 07. إدارة المفاتيح — إلغاء مفتاح

> ⬜ **قيد التعبئة** — يُلغى مفتاح API فوراً. المدفوعات المرتبطة لا تتأثر.

---

## 08. الدفع — إنشاء رابط دفع

> ⬜ **قيد التعبئة** — انظر المهمة M3 في `tasks/05-merchants/M3-payment-gateway.md` للتفاصيل الأولية.

---

## 09. الدفع — تأكيد الدفع

> ⬜ **قيد التعبئة** — معالجة الدفع عبر reference.

---

## 10. الدفع — إرجاع / مرتجع

> ⬜ **قيد التعبئة** — إرجاع كامل أو جزئي لمبلغ الدفع.

---

## 11. رمز الاستجابة السريع — توليد

### POST /api/v1/merchant/qr/generate

توليد رمز QR ديناميكي للدفع في المتجر الفعلي. الرمز صالح لمدة 15 دقيقة كحد أقصى. يتطلب Bearer Token للتاجر.

#### المدخلات

| الحقل | النوع | الإلزام | الوصف |
|-------|-------|--------|-------|
| `amount` | integer | مطلوب | المبلغ بأصغر وحدة عملة (فلس) — مثلاً 1500 = 15.00 USD |
| `currency` | enum(SYP, USD) | مطلوب | عملة الدفع |
| `order_id` | string(64) | اختياري | رقم الطلب لدى التاجر (إن وجد) |
| `branch_id` | string(32) | اختياري | معرف الفرع (للمتاجر متعددة الفروع) |
| `description` | string(128) | اختياري | وصف عملية الدفع |

#### الاستجابة (201 Created)

```json
{
  "success": true,
  "data": {
    "qr_id": "qr_f6e7d8a9",
    "transaction_id": "txn_f6e7d8",
    "qr_code_png": "data:image/png;base64,iVBORw0KGgo...",
    "qr_code_svg": "<svg xmlns=\"...\">...</svg>",
    "qr_raw_data": "{\"v\":1,\"mid\":\"merch_a1b2\",\"tid\":\"txn_f6e7d8\",\"a\":1500,\"c\":\"USD\",\"e\":\"20260531T141500Z\",\"s\":\"a1b2c3d4...\"}",
    "amount": 1500,
    "currency": "USD",
    "expires_at": "2026-05-31T14:15:00Z",
    "status": "pending"
  }
}
```

#### رموز الحالة

| الرمز | المعنى |
|-------|--------|
| 201 | تم توليد رمز QR بنجاح |
| 400 | خطأ في المدخلات — مبلغ صفر أو سالب، عملة غير مدعومة |
| 401 | غير مصرح — التوكن منتهي أو غير صالح |
| 403 | ممنوع — حساب التاجر معلق أو غير مفعل |

---

## 12. رمز الاستجابة السريع — التحقق

### POST /api/v1/merchant/qr/verify

التحقق من صحة رمز QR وبدء معالجة الدفع. يتطلب Bearer Token للمستخدم (وليس التاجر).

#### المدخلات

| الحقل | النوع | الإلزام | الوصف |
|-------|-------|--------|-------|
| `qr_data` | string | مطلوب | النص الداخلي للرمز (JSON كامل) |
| `device_fingerprint` | string(128) | اختياري | بصمة الجهاز الماسح |

#### الاستجابة — رمز صالح وغير مدفوع (200)

```json
{
  "success": true,
  "data": {
    "merchant_id": "merch_a1b2",
    "merchant_name": "متجر أحمد الإلكتروني",
    "amount": 1500,
    "currency": "USD",
    "description": "تيشيرت قطني",
    "order_id": "ORD-0042",
    "status": "valid",
    "expires_at": "2026-05-31T14:15:00Z",
    "payment_ready": true
  }
}
```

#### الاستجابة — رمز منتهي الصلاحية (400)

```json
{
  "success": false,
  "error": {
    "code": "QR_EXPIRED",
    "message": "انتهت صلاحية رمز QR. الرجاء طلب رمز جديد من التاجر.",
    "details": {
      "expired_at": "2026-05-31T14:15:00Z",
      "current_time": "2026-05-31T14:20:00Z"
    }
  }
}
```

#### الاستجابة — توقيع غير صالح (400)

```json
{
  "success": false,
  "error": {
    "code": "QR_INVALID_SIGNATURE",
    "message": "رمز QR غير صالح أو تم التلاعب به.",
    "details": {
      "reason": "signature_mismatch"
    }
  }
}
```

#### رموز الحالة

| الرمز | المعنى |
|-------|--------|
| 200 | رمز صالح — جاهز للدفع |
| 400 | رمز غير صالح — منتهي، مدفوع مسبقاً، توقيع خاطئ، أو ملغى |
| 401 | غير مصرح — التوكن منتهي |
| 404 | التاجر غير موجود أو الحساب معلق |

---

## 13. التسوية — سرد التسويات

> ⬜ **قيد التعبئة** — عرض سجل التسويات اليومية للتاجر مع المبالغ والعمولات.

---

## 14. التسوية — تفاصيل التسوية

> ⬜ **قيد التعبئة** — تفاصيل تسوية محددة: المعاملات المضمنة، الرسوم، صافي المبلغ.

---

## 15. التقارير — تقارير المبيعات

> ⬜ **قيد التعبئة** — تقارير مبيعات يومية/شهرية/مخصصة حسب الفترة.

---

## 16. التقارير — تقارير طرق الدفع

> ⬜ **قيد التعبئة** — توزيع المدفوعات حسب طريقة الدفع (محفظة، بطاقة، QR).
