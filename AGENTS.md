# AGENTS.md - دليل التشغيل الثابت للوكلاء الذكاء الاصطناعيين

منصة بيزا (Beza Platform) | https://github.com/es3dlll/beza-platform

**تحذير هام: هذا ملف دستوري تشغيلي. أي وكيل ذكي يعمل على هذا المستودع ملزم بالالتزام الحرفي بهذه الأوامر والشروط. أي انحراف يُعتبر خرقاً أمنياً ومعمارياً.**

---

## 1. الهوية والسياق الإلزامي

### 1.1 تعريف المشروع
- **الاسم:** منصة بيزا (Beza Platform)
- **النوع:** نظام تشغيل مالي رقمي وطني للجمهورية العربية السورية
- **الهدف:** تمكين 22 مليون مقيم و6 ملايين مغترب من خدمات مالية آمنة، شفافة، ومتوافقة مع المصرف المركزي السوري
- **العملة الأساسية:** الليرة السورية (SYP) بوحدات صغرى (فلس)

### 1.2 القيم غير القابلة للتفاوض
- **الثقة قبل السرعة** في كل قرار تقني أو تجربة مستخدم
- **الشفافية المالية المطلقة:** كل رسم، عمولة، وسعر صرف معروض قبل التأكيد
- **الامتثال الصارم:** تصميم يلبي متطلبات المصرف المركزي و AML/CFT منذ السطر الأول
- **العربية أولاً:** واجهات، توثيق، ومصطلحات بالعربية مع دعم كامل لاتجاه النص من اليمين لليسار
- **الأمان المدمج:** حماية البيانات والأرصدة جزء من النواة، ليس إضافة لاحقة

### 1.3 نطاق صلاحيات الوكيل
- **مسموح لك:** قراءة الكود، اقتراح تحسينات، إنشاء ملفات جديدة ضمن الهيكلية المعتمدة، كتابة اختبارات، تحديث التوثيق
- **ممنوع عليك:** تعديل المنطق المالي في CoreFinancialEngine أو Ledger دون موافقة صريحة، حذف سجلات، تجاوز حدود الوحدات، اقتراح حزم خارجية غير مدققة أمنياً

---

## 2. القواعد المعمارية الثابتة التي لا يجوز مخالفتها

### 2.1 هيكلية الوحدات النمطية الإلزامية
أي وحدة جديدة يجب أن تتبع هذا الهيكل حرفياً:
- **المسار الأساسي:** `app/Modules/{اسم_الوحدة}/`
- **المجلدات المطلوبة بالترتيب:**
  - `Actions`: عمليات أعمال قابلة لإعادة الاستخدام
  - `Controllers`: متحكمات رقيقة تستقبل الطلبات وتعيد الاستجابات فقط
  - `Events`: أحداث قابلة للإطلاق
  - `Exceptions`: استثناءات خاصة بالوحدة
  - `Listeners`: مستمعو الأحداث
  - `Models`: نماذج Eloquent مع ULID و Timestamps
  - `Policies`: سياسات صلاحيات تعتمد على السمات
  - `Rules`: قواعد تحقق مخصصة
  - `Services`: خدمات الأعمال كفئات نهائية
  - `ValueObjects`: كائنات القيمة مثل Money و Currency و Rate
  - `Database`: يحتوي على Factories و Migrations و Seeders
  - `Resources/lang`: ترجمات الوحدة تبدأ بالعربية ثم الإنجليزية والكردية والأرمنية
  - `Routes`: يحتوي على ملف api.php لمسارات الوحدة بادئة /v1/{اسم_الوحدة}
  - `Tests`: يحتوي على Feature و Unit
  - `Providers/ModuleServiceProvider.php`: ملف وحدة الخدمة
  - `README.md`: ملف التوثيق يشرح هدف الوحدة واعتماداتها وتدفقاتها الرئيسية

### 2.2 قاعدة التواصل بين الوحدات غير القابلة للكسر
- **القاعدة الذهبية:** لا استدعاءات مباشرة بين الوحدات مطلقاً
- **الطريقة الصحيحة:**
  - إطلاق حدث باستخدام `event(new اسم_الحدث(...))`
  - الاستماع للحدث عبر كلاس يطبق ShouldQueue مع دالة handle تستقبل الحدث
- **الطريقة الممنوعة:**
  - استدعاء service من وحدة أخرى مباشرة عبر `app()` أو الحقن
  - إنشاء نموذج أو استعلام مباشر على جداول وحدة أخرى
  - استدعاء Controller أو Route خاص بوحدة أخرى
- **الاستثناء الوحيد المسموح:** الوحدات المشتركة في `app/Modules/Core/` يمكن استدعاؤها عبر واجهات رسمية موثقة

### 2.3 قواعد البيانات والمالية الصارمة
- **قاعدة المفاتيح:** استخدام ULID حصرياً لكل الجداول الأساسية. ممنوع استخدام Auto-increment أو UUID غير مرتب زمنياً
- **قاعدة الأموال:**
  - التخزين في قاعدة البيانات: bigint بوحدات صغرى (فلس)
  - المعالجة في الكود: استخدام كائن القيمة Money حصرياً
  - ممنوع تماماً: float أو double أو decimal في أي عملية حسابية مالية
- **قاعدة دفتر الأستاذ (Ledger):**
  - هو مصدر الحقيقة الوحيد للأرصدة في النظام
  - يعمل بنظام Append-only و WORM (كتابة مرة واحدة وقراءة فقط)
  - ممنوع تماماً: UPDATE أو DELETE على أي سجل مالي في دفتر الأستاذ
  - طريقة الإلغاء: تتم عبر إنشاء قيد عكسي جديد، وليس عبر تعديل السجل الأصلي
- **قاعدة المحرك المالي المركزي (CFE):**
  - يملك حصرياً حالات الأرصدة وعمليات الخصم والإيداع
  - أي وحدة أخرى تريد تعديل رصيد يجب أن تطلب من CFE عبر واجهة عامة موثقة
  - ممنوع: كتابة أو تعديل رصيد مباشر في أي جدول غير جداول دفتر الأستاذ

### 2.4 قواعد الأمان والمصادقة الإلزامية
- **قاعدة المصادقة:**
  - استخدام JWT قصير العمر (15 دقيقة كحد أقصى) مع Refresh Token
  - ربط الجهاز Device Binding إلزامي لأي معاملة مالية
  - توفير OTP كخيار احتياطي عند فشل المصادقة البيومترية
- **قاعدة الصلاحيات:**
  - تطبيق نموذج RBAC + ABAC معاً
  - كل نقطة API يجب أن تحمي بسياسة صلاحية محددة
  - تسجيل كل محاولة وصول فاشلة في سجل التدقيق
- **قاعدة التشفير:**
  - تشفير البيانات الحساسة في قاعدة البيانات باستخدام AES-256
  - استخدام TLS 1.3 لجميع الاتصالات الشبكية
  - تخزين المفاتيح في بيئة معزولة غير قابلة للوصول من الكود
- **قاعدة سجل التدقيق:**
  - كل عملية مالية أو تغيير في الصلاحيات يجب أن تسجل في AuditLog
  - السجل غير قابل للتعديل أو الحذف (WORM)
  - يحتوي على: معرف المستخدم، الطابع الزمني، عنوان الجهاز، نوع العملية، النتيجة

---

## 3. خطوات التطوير الثابتة التي لا يجوز تجاوزها

### 3.1 قبل كتابة أي سطر كود جديد
1. اقرأ ملف `docs/architecture/CHARTER.md` لفهم القواعد المعمارية
2. تحقق من وجود وحدة مشابهة يمكن إعادة استخدامها قبل إنشاء وحدة جديدة
3. حدد الأحداث التي ستطلقها الوحدة والأحداث التي ستستمع لها
4. وثّق التغيير المقترح في ملف قرار معماري ADR إذا كان التغيير جوهرياً

### 3.2 أثناء كتابة الكود
- أضف `declare(strict_types=1);` في أول كل ملف PHP
- استخدم `final` للفئات التي لا يُتوقع ورثتها
- استخدم الحقن التبعية عبر البناء وليس عبر `app()` أو global helpers
- افصل المنطق التجاري عن المتحكمات والنماذج وضعه في Services أو Actions
- استخدم كائنات القيمة للبيانات المعقدة بدلاً من المصفوفات العشوائية
- اكتب التعليقات بالعربية عندما يكون السياق محلياً أو مالياً

### 3.3 بعد كتابة الكود وقبل الالتزام
1. شغّل المصحح الآلي: `composer format` أو ما يعادله
2. شغّل المحلل السكوني: `composer analyze` أو ما يعادله
3. اكتب اختباراً يغطي السلوك الجديد بنسبة تغطية لا تقل عن 90%
4. حدّث ملف مواصفات OpenAPI إذا أضفت أو عدلت نقطة API
5. حدّث ملف README الوحدة إذا غيرت السلوك أو أضفت ميزة

### 3.4 قبل طلب الدمج
1. تأكد أن جميع الاختبارات تمر: `php artisan test`
2. تأكد أن مواصفات OpenAPI محدثة: `php artisan openapi:generate && git diff --exit-code`
3. تأكد أن سجل التغييرات محدث: أضف سطراً في CHANGELOG.md يصف التغيير
4. املأ قالب طلب الدمج بالكامل مع وصف واضح للتأثير والاختبارات

---

## 4. معايير الاختبار الإلزامية

### 4.1 أنواع الاختبارات المطلوبة
- **اختبار الوحدة:** لكل دالة في Service أو Action، يغطي الحالات الطبيعية والحالات الحدية
- **اختبار التكامل:** لكل تفاعل بين وحدتين عبر الأحداث، يغطي نجاح وفشل التدفق
- **اختبار الميزة:** لكل رحلة مستخدم كاملة، يغطي السيناريو الأساسي والسيناريوهات البديلة
- **اختبار الامتثال:** لكل قاعدة AML أو KYC أو CBS، يغطي القبول والرفض والتعليق

### 4.2 قواعد كتابة الاختبارات
- استخدم أسماء دوال وصفية باللغة الإنجليزية: `test_user_can_transfer_money_if_balance_sufficient`
- رتب الاختبار: Arrange (الإعداد)، Act (التنفيذ)، Assert (التحقق)
- اعزل قاعدة البيانات لكل اختبار باستخدام RefreshDatabase
- استخدم المصانع Factories لإنشاء البيانات التجريبية
- تحقق من إطلاق الأحداث باستخدام `Event::fake()` و `Event::assertDispatched()`
- تحقق من سجل التدقيق عند اختبار العمليات المالية

### 4.3 عتبات الجودة الإلزامية
- نسبة تغطية الاختبارات: 90% كحد أدنى للوحدات الجديدة
- عدد التأكيدات لكل اختبار ميزة: 3 كحد أدنى
- وقت تنفيذ مجموعة الاختبارات: 5 دقائق كحد أقصى
- نسبة نجاح الاختبارات في CI: 100% إلزامي للدمج

---

## 5. معايير التوثيق الإلزامية

### 5.1 توثيق الوحدة النمطية
كل وحدة جديدة يجب أن تحتوي على ملف `README.md` يحتوي على:
- هدف الوحدة في جملة واحدة
- قائمة الاعتمادات: الوحدات التي تعتمد عليها والوحدات التي تعتمد عليها
- مخطط التدفق الرئيسي: نصي أو رابط لمخطط
- نموذج البيانات: الجداول الرئيسية والعلاقات
- نقاط API: جدول يحتوي على الطريقة، المسار، الوصف، المصادقة
- معالجة الأخطاء: قائمة الاستثناءات ورموز الاستجابة
- أمثلة الاستخدام: طلب واستجابة لنقطة API رئيسية

### 5.2 توثيق نقاط الواجهة البرمجية
كل نقطة API جديدة يجب أن:
- تضاف إلى ملف مواصفات OpenAPI في `docs/specs/openapi.yaml`
- تحتوي على وصف واضح بالعربية والإنجليزية
- تحدد جميع معاملات الطلب مع الأنواع والقيم المسموحة والإلزامية
- تحدد جميع استجابات النجاح والخطأ مع أمثلة
- تحدد متطلبات المصادقة والصلاحيات

### 5.3 توثيق القرارات المعمارية
أي تغيير جوهري في المعمارية يجب أن:
- يوثق في ملف ADR جديد في `docs/architecture/adr/`
- يحتوي على: السياق، القرار، البدائل المرفوضة، العواقب
- يربط بالوحدات المتأثرة والتغييرات المطلوبة
- يوقع من قائد التقنية ومسؤول الامتثال

---

## 6. بروتوكول التعامل مع الأخطاء والحوادث

### 6.1 تصنيف الأخطاء
- **خطأ حرج:** يؤثر على دقة الأرصدة أو أمان البيانات أو الامتثال التنظيمي
- **خطأ عالي:** يؤثر على تجربة المستخدم أو توفر ميزة رئيسية
- **خطأ متوسط:** يؤثر على أداء غير حرج أو واجهة ثانوية
- **خطأ منخفض:** مشكلة تجميلية أو تحسين مقترح

### 6.2 بروتوكول الاستجابة للأخطاء الحرجة
1. سجل الخطأ فوراً في سجل التدقيق مع كل السياق المتاح
2. أوقف العملية المتأثرة إن أمكن دون التأثير على العمليات الأخرى
3. أطلق حدثاً للتنبيه الفوري لفريق الطوارئ
4. لا تحاول إصلاح الخطأ تلقائياً إذا كان يتعلق بأرصدة أو امتثال
5. انتظر تعليمات الفريق البشري قبل أي إجراء تصحيحي

### 6.3 بروتوكول الاستعادة من الفشل
1. حدد نقطة الفشل بدقة من سجلات التدقيق
2. تحقق من سلامة البيانات المتأثرة عبر مقارنة مع دفتر الأستاذ
3. إذا لزم الأمر، أنشئ قيداً عكسياً موثقاً بدلاً من تعديل السجلات
4. وثّق سبب الفشل والإجراء التصحيحي في سجل الحوادث
5. حدّث الاختبارات لمنع تكرار نفس الخطأ

---

## 7. بروتوكول التحديث والصيانة

### 7.1 تحديث التبعيات الخارجية
1. راجع سجل التغييرات لكل حزمة محدثة للتحقق من التوافق والأمان
2. شغّل الاختبارات الكاملة بعد كل تحديث تبعية
3. حدّث ملف التوثيق إذا تغير سلوك معتمد على التبعية
4. لا تحدّث تبعيات الإنتاج مباشرة في فرع main، استخدم فرع التطوير أولاً

### 7.2 تحديث قواعد الامتثال
1. استلم تحديث القواعد من مصدر رسمي موثق (المصرف المركزي)
2. حدّث قواعد التحقق في الوحدة المعنية مع الحفاظ على التوافق العكسي إن أمكن
3. أضف اختبارات تغطي القاعدة الجديدة والحالات الحدية
4. وثّق التحديث في سجل الامتثال مع تاريخ السريان والمرجع الرسمي

### 7.3 تحسين الأداء
1. حدد نقطة الاختناق عبر مقاييس الأداء وليس التخمين
2. طبق التحسين في فرع منفصل مع اختبارات أداء قبل وبعد
3. تأكد أن التحسين لا يؤثر على دقة البيانات أو الامتثال
4. وثّق التحسين وسببه ونتائجه في ملف التحسينات

---

## 8. بروتوكول التسليم والنشر

### 8.1 قبل النشر للتجريب
1. تأكد أن جميع الاختبارات تمر بنسبة 100%
2. تأكد أن مواصفات OpenAPI محدثة ومتوافقة مع الكود
3. تأكد أن وثائق المستخدم محدثة لأي ميزة جديدة
4. أنشئ وسم إصدار تجريبي بصيغة: `v{major}.{minor}.{patch}-beta.{num}`

### 8.2 قبل النشر للإنتاج
1. نفذ اختبارات الحمل للتأكد من استقرار الأداء تحت الحمل المتوقع
2. راجع خطة التراجع وتأكد من إمكانية الاستعادة خلال 5 دقائق
3. درّب فريق الدعم على أي واجهات أو سلوكيات جديدة
4. أنشئ وسم إصدار نهائي بصيغة: `v{major}.{minor}.{patch}`

### 8.3 بعد النشر
1. راقب المؤشرات الحيوية لمدة 24 ساعة: الأخطاء، زمن الاستجابة، معدل الاستخدام
2. اجمع ملاحظات المستخدمين الأوليين وصنفها حسب الأولوية
3. حدّث وثائق التشغيل بأي دروس مستفادة من النشر الفعلي
4. خطط للتحديث التالي بناءً على الملاحظات والمؤشرات

---

## 9. قائمة المراجعة السريعة قبل أي التزام

قبل تنفيذ `git commit`، تأكد من النقاط التالية:

- [ ] الكود يحتوي على `declare(strict_types=1)` في أول كل ملف PHP
- [ ] لا توجد استدعاءات مباشرة بين الوحدات، كل شيء عبر الأحداث
- [ ] الأموال تعالج عبر كائن القيمة Money فقط، لا أنواع عائمة
- [ ] المفاتيح الأساسية من نوع ULID، لا Auto-increment
- [ ] سجل التدقيق يسجل كل عملية مالية أو تغيير صلاحيات
- [ ] الاختبارات مكتوبة وتغطي السلوك الجديد بنسبة 90%+
- [ ] مواصفات OpenAPI محدثة إذا تغيرت واجهة البرمجة
- [ ] التوثيق محدث: الوحدة، نقاط الـ API، وسجل التغييرات
- [ ] لا توجد أسرار أو مفاتيح في الكود، كلها في متغيرات البيئة
- [ ] الالتزام يتبع الصيغة التقليدية: `feat(scope): description`

---

## 10. قنوات التواصل والدعم للوكيل

للأسئلة أو الاستفسارات حول هذه القواعد:
- راجع أولاً: `docs/architecture/CHARTER.md`
- ثم: `docs/operations/QUICK_REFERENCE_OPERATORS.md`
- للأسئلة التقنية: قناة #eng-ledger على Slack
- للأسئلة الامتحائية: قناة #compliance-ops على Slack
- للطوارئ التقنية: اتصل برقم الطوارئ أو استخدم قناة #incident-response

للإبلاغ عن مشكلة في هذه القواعد نفسها:
- أنشئ قضية في المستودع بعنوان: `[AGENT-DOCS] وصف المشكلة`
- أضف وسم: `documentation`, `agent-protocol`
- اذكر في الوصف: القاعدة غير الواضحة، السياق، والاقتراح للتحسين

---

## ختام وإقرار

أنا الوكيل الذكي أقر بأنني:
- قرأت وفهمت جميع القواعد والشروط في هذا الملف
- سألتزم بها حرفياً في أي عمل أقوم به على هذا المستودع
- سأبلغ فوراً عن أي غموض أو تعارض في هذه القواعد
- سأرفض تنفيذ أي طلب يتعارض مع هذه القواعد حتى مع وجود إذن بشري، وسأطلب توضيحاً رسمياً

---

## ملحق: أمثلة عملية سريعة

### مثال 1: إنشاء وحدة جديدة
1. أنشئ المجلد: `app/Modules/NewModule/`
2. أنشئ المجلدات الفرعية الـ 16 المطلوبة
3. أنشئ ملف الخدمة: `Providers/ModuleServiceProvider.php` وسجله في `bootstrap/providers.php`
4. أنشئ ملف التوثيق: `README.md` بالهيكل المطلوب
5. ابدأ بإضافة أول ميزة مع اختبارها وتوثيقها

### مثال 2: إضافة نقطة API جديدة
1. حدد المسار والطريقة والوصف بالعربية والإنجليزية
2. أضف النقطة في ملف `Routes/api.php` للوحدة مع البادئة `/v1/{module}`
3. أنشئ المتحكم والدالة مع التحقق من الصلاحيات
4. أضف النقطة في ملف OpenAPI مع جميع التفاصيل
5. اكتب اختباراً يغطي النجاح والخطأ والصلاحيات
6. حدّث ملف README.md للوحدة بجدول نقاط الـ API

### مثال 3: معالجة معاملة مالية
1. استلم الطلب في المتحكم مع التحقق من المصادقة والصلاحيات
2. تحقق من صحة المدخلات باستخدام كائنات القيمة
3. أطلق حدث طلب المعاملة للوحدة المالية المركزية
4. استمع لحدث نجاح أو فشل المعاملة
5. حدّث واجهة المستخدم وأضف العملية لسجل التدقيق
6. أطلق حدثاً للوحدات الأخرى التي تحتاج للمعرفة (إشعارات، تسوية، امتثال)

---

> **هذا الملف ساري المفعول فوراً. أي وكيل يبدأ العمل على هذا المستودع بعد هذا التاريخ ملزم بالامتثال الكامل لهذه القواعد. التحديثات المستقبلية لهذا الملف ستتبع نفس بروتوكول القرارات المعمارية وتُعلن عبر قناة #eng-ledger.**
